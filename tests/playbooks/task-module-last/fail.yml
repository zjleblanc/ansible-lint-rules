---
- hosts: all
  tasks:
    - name: Example task with when condition
      ansible.builtin.dnf:
      become: true

    - name: Example block with when condition
      block:
        - name: Example task
          ansible.builtin.dnf:
      become: true
          
